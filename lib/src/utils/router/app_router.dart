/// Конфиг go_router (заготовка).
///
/// После добавления пакета `go_router`:
/// — `MaterialApp.router` в `src/app.dart`;
/// — `GoRouter` + `StatefulShellRoute` здесь;
/// — SMART shell (`nav_shell.dart`) рядом.
library;
